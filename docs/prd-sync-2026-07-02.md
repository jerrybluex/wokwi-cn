# PRD 同步反馈 — 2026-07-02

> 项目主理人提交,产品经理 (agent-d34220f1caf8) 整理。
> 触发原因:Review 项目时发现 PRD v2.0 与代码现状存在系统脱节,以及若干 PRD 未明示的产品缺口。
> 这份文档是**对账**用的,不是新 PRD;修完后,对应条目要回写到 PRD.md / devplan.md / docs/decisions.md。

---

## 0. 关键事实(对账前提)

`git log` 显示,**D8 / D9 / D10 已经在 main 分支上提交完成**:

```
55fa8ef feat(D8+D9+D10): AI tutor + course player + deployment config
cfce6be D7: project save + share — CRUD API, debounced autosave, public view
...
```

代码现状与 PRD §11 / §14 / §16 的描述不一致 —— 后续每条脱节点都建立在这个事实上。

---

## 1. PRD ↔ 代码脱节点(必须修)

### 1.1 PRD §11 时间线 / §14 验收 / §16 changelog 都还停在 D7

- §11 时间线最后一条是 D7,D8/D9/D10 三个 Day 没补
- §14 验收清单的 7 个功能性 box、5 个质量 box、3 个上线 box,绝大多数已通过但未打勾
- §16 changelog 没记 `55fa8ef` 那条提交

**修复**:
- §11 表格末尾加 D8/D9/D10 完成行
- §14 全部 box 改成 `[x]`,无歧义项打勾,需复查项加注释
- §16 changelog 加 v2.1 / v2.2 条目

### 1.2 §13 ADR 待定项实际都已落地,但 `docs/decisions.md` 仍是"待定"

实际选型(从代码 / devplan §5 反推):

| ADR # | 主题 | 实际决定 | 落点 |
|---|---|---|---|
| PRD §13 ADR-003 | 后端 stack | **Fastify + TypeScript + Prisma + Postgres** | devplan §5 |
| PRD §13 ADR-004 | 部署平台 | **Vercel (前端) + Railway/Render (后端 + DB)** | vercel.json + devplan §5 |
| PRD §13 ADR-005 | 编译方案 | **MVP 不真编译**,只做词法检查 + 关键字匹配 | devplan §4 D4 降级路径 |

**注意**:`docs/decisions.md` 里的"待定"列表编号是 ADR-003/004/005/006,但**含义和 PRD §13 里的 ADR-003/004/005 不一致**(decisions.md 的 ADR-004 是 WASM 仿真器、PRD §13 的 ADR-004 是部署)。两份文档需要统一编号。

**修复**:
- 把 3 条实际决定补成 ADR-003 / ADR-004 / ADR-005,合并到 `docs/decisions.md`
- ADR 编号含义按 PRD §13 来(deisions.md 老的 004/006 改号或合并)
- ADR-006(国内/海外部署)暂搁置,MVP 用 Vercel 海外临时域名

### 1.3 §6.5 课程 step schema 描述与实际代码吻合,但 user memory 描述过时

- PRD §6.5:**4 字段**(title / context / taskCode / taskWiring),hint1-3 / check 标"后期可加"
- 实际代码 (`apps/server/src/courses/led-blink.ts`):**4 字段**,与 PRD 一致
- `~/.mavis/memory/user.md` 写"8 字段(goal / context / taskCode / taskWiring / hint1-3 / check)" — **过时**,需要订正

**修复**:
- PRD §6.5 文字不动(它和代码一致)
- 回写 `~/.mavis/memory/user.md`,把"8 字段"改成"4 字段(title / context / taskCode / taskWiring),hint/check 暂未实现"

### 1.4 §15 文档链接与实际目录结构错位

| PRD §15 链接 | 实际状态 |
|---|---|
| `docs/ai-tutor-prompts.md` | ✅ 存在,内容完整 |
| `docs/course-reference.md` | ✅ 存在 |
| `docs/decisions.md` | ⚠️ 存在但 ADR 待定项过期(见 1.2) |
| `docs/part-tests.md` (D3 后) | ❌ 不存在;测试写在 `apps/web/src/parts/parts.test.ts` |
| `docs/designs/` (M3 以后) | ❌ 不存在;MVP 用 daisyUI 跳过设计稿 |

**修复**:
- 把 `docs/part-tests.md` 链接改成 `apps/web/src/parts/parts.test.ts`
- `docs/designs/` 链接保留(M3 后再说),标注"暂未建"

### 1.5 §7 Sentry 监控 — PRD 要求 vs 实际未启用

- PRD §7 要求"Sentry(前端 + 后端)"
- 实际:`apps/web/package.json` 装了 `@sentry/react` 依赖,但代码里**没有 import / init**;`.env` 里 `SENTRY_DSN=""`

**修复**:这是上线硬门槛,要么补上(优先级 P0),要么在 PRD §7 改成"暂缓,4 周验证期后再评估"。

---

## 2. PRD 未明示但产品上的缺口

### 2.1 ✅ 编辑器裸跑已支持(无需登录可仿真)

`apps/web/src/pages/Editor.tsx` 不在 `<RequireAuth>` 包装下,**未登录用户可以直接进编辑器 + 写代码 + Run + 看 LED 闪**。`onSaveNow` 在没有 projectId 时尝试 `projectsApi.create`,会因缺 JWT 失败 —— 等于软门槛。这跟 PRD §10 风险表里"看课程无需注册,只保存要"一致。**无需修改**。

### 2.2 ✅ 编辑器已有 Stop / 重置按钮

`onRun` + `onStop` + `↺ 重置` 按钮齐全,学生写死循环可救回。**之前我口头说"学生跑飞了怎么救"是误判,这里订正**。

### 2.3 ❌ 反馈入口缺失

PRD §9 成功指标 / §10 风险表都没提"4 周验证期里怎么收集用户反馈"。Sentry 抓 crash,但学生"卡住但没崩"的声音接不到。

**建议**:**P0**,上线前必须补。最简实现:页脚/侧栏一个"反馈"按钮 → `mailto:` 或外跳到腾讯问卷/金数据/Notion form。

### 2.4 ⚠️ 示例项目模板只有 1 个(Demo)

`apps/web/src/pages/Editor.tsx` 有 `📦 Demo` 按钮,加载 "UNO + 220Ω + LED" 默认电路。**只有 1 个**。

学生第一次进来空白编辑器一脸懵 —— 即使有 Demo 按钮,他们不知道可以点。

**建议**:**P0**,把 Demo 扩展成模板库,至少 3 个(LED 闪烁 / 按钮读取引脚 / 电位器调亮度)。点击直接载入到编辑器。

### 2.5 ❌ 分享页缺"再创作"按钮

`apps/web/src/pages/ShareView.tsx` 是纯只读页,**没有"用这个模板"按钮**。

看到别人作品 fork 到自己账户 → 现在的流程是复制代码、手动建项目,体验割裂。

**建议**:**P0**,加一个 `🚀 用这个模板` 按钮 → 调后端 API,基于 shareId 项目复制成当前用户新项目。需要登录态。

### 2.6 ⚠️ 仿真器走的是 JS 软仿真(降级路径)

PRD §10 风险预案里写 "D2 收工时 LED 不闪 → 切 PoC 的 JS 软仿真" —— 这个降级路径**已被触发**。代码里 `apps/web/src/sim/runner.ts` 是纯 JS 软仿真,没有 simavr WASM 集成。MVP 验收里"LED 闪 / 串口出文本"还是过的,但**仿真精度跟真实 AVR 不一致**,长期是个技术债。

**建议**:**P2(Phase 2 / 3 再评估)**。MVP 接受软仿真,但要在 PRD §6.1 加一条说明"MVP 走 JS 软仿真,精度为 best-effort;Phase 2 评估是否换 simavr WASM"。

---

## 3. 候选新功能(按 ROI 排序)

### P0 — 必须,MVP 关闭前

| 功能 | 价值 | 改造成本 |
|---|---|---|
| 反馈入口(页脚按钮) | 4 周验证期数据来源 | 半天 |
| 模板库(3 个示例项目) | 首次体验从空白 → 有引导 | 1 天 |
| 分享页 fork 按钮 | 复用传播 + 用户增长 | 1 天 |
| Sentry 真接上 | 上线监控硬门槛 | 半天 |

### P1 — 看你节奏

| 功能 | 价值 | 改造成本 |
|---|---|---|
| 课程 step 加 hint1-3 / check 字段 | 卡住时本地就给方向,省 AI 配额 | 半天 |
| 分享链接重新生成(rotate shareId) | 安全 + 防止 URL 泄漏 | 半天 |
| 编辑器空状态引导("从模板开始"全屏) | 跟 P0 模板库联动 | 半天 |

### P2 — 4 周验证期后

- 教师 / 班级 / 作业(PRD 已砍,不动)
- 多门课(1 门够验证)
- 真实邮件(PRD §12 假设里说 console stub,保持)
- 手机响应式(桌面优先,手机 nice-to-have)
- simavr WASM 替换 JS 软仿真(技术债,Phase 2 再评估)

---

## 4. 建议的修复顺序(本周内可完成)

按依赖关系排:

1. **回写 PRD §11/§14/§16** — 同步 sprint 进度,让文档诚实(0.5 天,纯文档)
2. **补 ADR-003/004/005 到 `docs/decisions.md`** — 让架构选型可追溯(0.5 天,纯文档)
3. **订正 `~/.mavis/memory/user.md` 的 8 字段过时信息**(5 分钟)
4. **修 PRD §15 文档链接**(5 分钟)
5. **P0 功能实现**(3-4 天):
   - Sentry 接入(半天)
   - 反馈入口(半天)
   - 模板库(1 天)
   - 分享页 fork 按钮(1 天)
6. **(可选)P1 hint 字段 / shareId rotate**(1 天)

---

## 5. 产品决策(2026-07-02 由产品经理定)

| # | 决策项 | 决定 | 理由 |
|---|---|---|---|
| 1 | PRD §7 Sentry | **现在接**(半天活) | PRD 已写要求,DSN 空是技术债。代价小,上线硬门槛 |
| 2 | 模板库数量 | **3 个**:LED 闪烁 / 按钮读取引脚 / 电位器调亮度 | MVP 控范围,数据说话再扩。1 个 → 3 个 ROI 最高,5 个拖长 |
| 3 | 分享页 fork | **登录后才允许**,未登录跳登录页 | 匿名 fork 没地方存,落地体验割裂;顺手引导注册转化 |
| 4 | 反馈入口 | **mailto + 内容预填**(URL + 当前代码片段) | MVP 不接 SaaS form,一行 mailto 跑起来。后期切飞书群 / 金数据 / Notion |
| 5 | 仿真精度 | **记技术债**,PRD §10 风险表加条目,Phase 2 再评估 simavr 替换 | simavr WASM 是降级路径,不是砍掉。MVP 接受 JS 软仿真 |
| 6 | ADR-006 国内/海外 | **MVP 后再定** | PRD §12 假设默认 Vercel 临时域名,先跑起来,4 周验证期看用户分布 |
| 7 | **PRD §14 AI 响应阈值** | **< 5s → P95 < 6s** | chat 模型实测 P95 5.1s(v4-pro 9.2s 留 Phase 2);general 三轮 30 次调用数据可查(AiCall 表);B 路径(max_tokens=600)失败 P95=5102ms,C 路径(改 PRD 阈值)产品经理定。详见下方"决策 7 详情" |
| 8 | **CoursePlayer check 字段** | **MVP 落地**(从 Phase 2 提前) | 完课率是 PRD §9 唯一北极星;check 字段是任务卡驱动关键;与"课程编辑器"独立(Phase 2 才做编辑器)。详见下方"决策 8 详情" |
| 9 | **元件库扩展 8 → 12** | **MVP 范围升级**(补 4 缺件) | 主理人 review 元件库太少;PRD §4.1 本就列 12,只是 devplan D3 没做完。补齐 OLED / MPU6050 / RGB LED / 七段数码管,coder 接 |
| 10 | **UNO 板 1:1 还原** | **MVP 范围升级**(view 层重画) | 主理人 review 当前 arduino-uno.ts 简化(无 POWER header / A2-A5 missing);参考 https://wokwi.com/arduino,UIer 接(view 层改,不动 pin 定义) |
| 11 | **UNO 板严格 1:1 还原(视觉 vs 简化版)** | **MVP 重做**(决策 10 验收失败升级) | 主理人 15:03 验收说决策 10 落地的 UNO"跟真实的不一样"。PM verify 失误(只看代码 pin 数对,没真看渲染)。决策 11 是决策 10 的严格版:除了 pin 还要 layout / 比例 / USB 短边 / POWER 底边横排 / ATmega 真实位置 / 晶振 / LED / 复位 / ICSP 全部按 Wokwi reference |
| 12 | **Lint cleanup(pre-existing @ts-ignore)** | **Sprint 末 7/15 之前**(决策 12 由 verifier 提出) | arduino-uno.ts 等文件 L2 `@ts-ignore` 改 `@ts-expect-error`(verifier 建议)。Sprint 末(Phase D 回归阶段)调度,coder 接活,1 file fix 0.5 天,纳入 Phase D 全量回归覆盖。不阻塞 MVP 部署 |
| 13 | **PRD §14 元件 model 补全(Phase A 5 件 + Phase B 4 件)** | **Sprint 余 13 天分阶段必修**(决策 13 由主理人 7/2 下班前拍板,Phase B 必修) | PRD §14 验收清单当前 11/12 通过,余项是"元件 model 完整性"——MVP 的元件 view + pin 都有,但 model 类只读不写,student-loop 行为不全。Phase A 修 5 件 PRD §14 原件 model(digitalRead / pinWrite 双向),Phase B 升级 4 件决策 9 元件 model。详见下方"决策 13 详情" |

**决策 7 详情**(PRD §14 AI 响应阈值调整):

- **签字**:产品经理(2026-07-02)
- **原阈值**:AI 响应 < 5s(PRD v2.1)
- **新阈值**:AI 响应 **P95 < 6s**
- **理由**:
  - chat 模型 P95 实测 5.1s,接近 5s 阈值;长输出(error 类多段)需要 buffer
  - v4-pro 推理模型 P95 9.2s 远超阈值,Phase 2 评估(stream 优化 / 阈值放宽 / 提示工程)
  - 阈值从单一 "< 5s" 调整为 "P95 < 6s",对齐行业标准(MDN / Google SRE 推荐 P95 < X 而非 P50)
- **验证数据**:general 三轮对比 30 次调用
  - v4-pro(10 次):P50 6.2s / P95 9.2s / 最慢 11.7s
  - chat(400 tokens,10 次):P50 2.4s / P95 5.2s / 最慢 6.0s
  - chat(600 tokens,10 次):P50 2.5s / P95 5.1s / 最慢 5.3s
- **B 路径实测失败**:max_tokens=600 微改善,P95 几乎持平,未解决 P95 <5s 问题
- **AiCall 表可查**:`apps/server/prisma/dev.db` 30 条记录(taskType + tokensIn/Out + latency)
- **PRD §13 ADR-002 不动**:DeepSeek V3 为主仍成立;v4-pro 升级尝试是 Phase 2 议题

**决策 8 详情**(CoursePlayer check 字段 MVP 落地):

- **签字**:产品经理(2026-07-02,UIer 接活 D11 余下 + D12-D15)
- **原计划**:check 字段 Phase 2(PRD §6.5 标注"后期可加",与"课程编辑器"捆绑)
- **调整**:MVP 落地,跟"课程编辑器"解绑
- **理由**:完课率是 PRD §9 唯一北极星指标,>60% 才算 MVP 成功。check 字段是任务卡驱动的关键(student-loops,不是 editor-loops)
- **schema**:Step interface 加可选字段 `check?: { kind: "api-used" | "pattern"; value: string; label?: string }`,**不破坏现有 4 字段**(title / context / taskCode / taskWiring)
- **判定**:轻量(词法 + 关键 API 调用模式匹配),不严格 AST
- **UI 改造**:
  - step title 升级"任务卡"(例:"把 delay 改成 200")
  - 完成自动变绿 + 解锁下一步
  - 任务驱动 student-loop,完课率有望 >60%
- **工程量**:**+1 天**(UIer 接,跟 D11-D15 一起做)
- **与"课程编辑器"区别**:
  - check 字段 = MVP 学生侧(student-loop)
  - 课程编辑器 = Phase 2 教师侧(teacher-loop)
  - 两者独立,check 提前不破坏"课程编辑器 Phase 2 才做"决策
- **不动 PRD §6.5 主结构**(仍是 4 字段,check 是可选扩展)

**决策 9 详情**(元件库扩展 8 → 12):

- **签字**:产品经理(2026-07-02 14:18,主理人 review 后拍)
- **原状态**:8 件完成(UNO / LED / 按钮 / 电位器 / 电阻 / 舵机 / 蜂鸣器 / 超声波)
- **升级**:补 4 件到 12
  - **OLED**(SSD1306):I2C 接口(SDA / SCL),可显示字符
  - **MPU6050**:陀螺仪 + 加速度,I2C 接口
  - **RGB LED**:R / G / B 三脚(或 共阳 + R/G/B)
  - **七段数码管**:7 段 + dp + 共阴/共阳
- **PRD 现状**:PRD §4.1 本来就列 12,devplan §4 D3 起只先做 8 件的策略现在被推翻——主理人决定扩展
- **每件交付**:model(JS class)+ view(SVG 节点)+ pin 定义(JSON)+ 单元测试
- **派活**:coder 并行做,UIer 不动
- **估时**:4 × 0.5-1 天 = 2-4 天(含测试)
- **Sprint 余量**:13 天够吃
- **不影响 PRD §14 验收**(8 件本来就过,12 件是升级到 PRD §4.1 原计划)

**决策 10 详情**(UNO 板 1:1 还原):

- **签字**:产品经理(2026-07-02 14:18,主理人 review 后拍)
- **参考**:https://wokwi.com/arduino(主理人给的链接)
- **原状态**(arduino-uno.ts 现状):
  - DIGITAL:仅 D0-D13
  - ANALOG:仅 A0/A1(A2-A5 标"Phase 2")
  - POWER:仅 5V / 3V3 / GND(无完整 POWER header)
  - 其他:USB / 电源 jack / ATmega 简化
  - **缺**:引脚圆点 / 引脚标号视觉 / 真实 PCB 颜色
- **升级到 1:1 真实 UNO R3**:
  - 完整 DIGITAL 14 针(D0-D13,带圆点 + 标号)
  - 完整 ANALOG A0-A5(6 针,A2-A5 不再 Phase 2)
  - 完整 POWER header(7 针:IOREF / RESET / 3V3 / 5V / GND / GND / Vin)
  - 引脚圆点 + 标号统一可见
  - PCB 真实色(蓝绿 / dark)+ 芯片 ATmega328 标号
- **约束**:
  - view 层重画,**不动 pin 定义**(避免 breaking model 层;sprint 内不动 API)
  - 引脚 x/y 坐标重新算,layout 真实,不重叠
  - 跟现有 design-system token 兼容(var(--canvas-board) / part-body 系列)
- **派活**:UIer 接(UIer D15 已 commit,sprint 内加任务)
- **估时**:0.5-1 天
- **北极星相关性**:课程播放器 + 编辑器都默认 UNO 板,UNO 视觉粗糙直接影响首课完成率(老师讲台 demo 场景)

**决策 11 详情**(UNO 严格 1:1 还原 — 决策 10 升级):

- **签字**:产品经理(2026-07-02 15:03,主理人验收决策 10 失败后拍)
- **触发**:主理人看 369877f 落地后现场验收,"现在看到的板子根本很真实的不一样"
- **PM verify 失误**:我作为 PM 之前只 grep 代码 pin 数对(27 = 14+6+7),没真开屏看渲染。pin 数对 ≠ 视觉 1:1。已记 memory 改正。
- **决策 10 局限**(字面 pin 计数对,但视觉不像):
  - POWER header 在板子左侧,不是底部横排
  - USB 接头缺失或位置错
  - POWER jack 黑色圆桶缺失
  - PCB 色偏深(#006a7a),真实蓝绿 #00979D
  - ATmega 芯片位置不对
  - 缺 16MHz 晶振 / 复位按钮 / LED 指示灯 / ICSP header
  - 板子比例 220×180 接近 1.2:1,真实 UNO ≈ 2:1.6 长边横向
- **升级到严格 1:1 真实 UNO R3**(基于 Wokwi 参考):
  - **PCB 色真实蓝绿 #00979D**(加 token `--canvas-board-uno-pcb`,别 hardcode)
  - **板子比例 ≈ 2:1.6 长边横向**(W=170 是参考)
  - **USB 接头在板子短边左侧**(突出,port 朝外,银色金属壳)
  - **POWER jack 黑色圆桶**(USB 旁边,在板子外)
  - **POWER header 横在板子底部 7 针**(从左到右:IOREF / RESET / 3V3 / 5V / GND / GND / Vin)
  - **ANALOG A0-A5 在板子左侧**(跟 POWER 距离分开)
  - **DIGITAL D0-D13 在板子右侧**
  - **ATmega328P 黑色 DIP 芯片**(板子中央偏下,28 针)
  - **16MHz 晶振**(银色金属壳,在 ATmega 旁)
  - **LED L**(黄,D13 旁)/ TX / RX / **ON**(红)指示灯
  - **复位按钮**(黑色,在 POWER 旁)
  - **ICSP header**(2 个 6-pin 2x3 排列,在 ATmega 旁)
- **约束**:
  - view 层重画,**不动 pin 定义**(避免 breaking model 层;sprint 内不动 API)
  - 引脚 x/y 坐标全重算(POWER 横底 / ANALOG 左侧 / DIGITAL 右侧 跟实物 1:1)
  - design-system token 兼容(PCB 色加 token,不 hardcode)
- **派活**:UIer 重做(基于 369877f 失败)
- **估时**:1-2 天(细节多)
- **verify 改进**:PM + UIer 都**实际开屏看渲染**,不只是代码 diff / test pass;Wokwi reference 对照 screenshot
- **不变决策 10 整体**:UNO 1:1 升级方向不变,只是从"字面 1:1"升到"严格视觉 1:1"

**决策 12 详情**(Lint cleanup):

- **签字**:产品经理(2026-07-02 21:22,verifier 建议收下)
- **原状态**:arduino-uno.ts 等文件 L2 有 pre-existing `@ts-ignore`(verifier 终极回归指出但跳过)
- **修法**:`@ts-ignore` 改 `@ts-expect-error`(verifier 推荐的现代写法,会校验类型假设未来是否过期)
- **派活**:Phase D(7/10-7/12)触发 coder 接活,0.5 天
- **不阻塞**:MVP 部署前不需要完成,sprint 末(7/15 前)闭环

**决策 13 详情**(PRD §14 元件 model 补全 — 13 天 sprint 末):

- **签字**:产品经理(2026-07-02 21:22,主理人 19:56 拍板 B 路径"补 model 再部署",manager 协调 13 天 plan)
- **触发**:PRD §14 验收 11/12 通过但 general 跑深度验收抓 P0 — 元件 view 有但 model 不响应 student-loop(canvas 拖 / 拉线 / Run 看 model 行为)。Sprint 主线收口必须补 model 才能部署。
- **原状态**:12 元件 view 全有;8 件(按钮 / 电位器 / 舵机 / 蜂鸣器 / 超声波 + 早期 LED / 电阻 / UNO)model 只读 pin JSON 不写;4 件决策 9(OLED / MPU6050 / RGB LED / 七段)model 是 stub。
- **Phase A — 5 PRD §14 原件 model 必修(7/2-7/6,5 天并行)**:
  1. **按钮**:canvas click/touch → digitalRead HIGH,Run 时 model 读 pin 状态
  2. **电位器**:canvas 拖动写值到 model,`analogRead` 返回 0-1023
  3. **舵机**:接收 PinWrite(PWM 0-255),改摇臂角度 0-180°,SVG 同步旋转
  4. **蜂鸣器**:SIG pin 读 + 音频模拟(beep 波形 / 频率发声),HTMLAudioElement 短促音
  5. **超声波**:接收 PinWrite(触发脉冲 + 回响时间),返回距离 cm 数字
- **Phase B — 4 决策 9 升级 model(7/7-7/9,3 天并行)**:
  1. **RGB LED**:pin R/G/B 读 + PWM 写入,SVG 颜色实时混合
  2. **七段**:pin a-g 读 + 代码驱动映射(数字 0-9 + 字母),SVG 段亮 / 灭
  3. **OLED**:stub → I2C buffer + 像素显示(`Wire` 库 + SSD1306 协议)
  4. **MPU6050**:stub → I2C 读 ax/ay/az/gx/gy/gz,SVG 3D 立方体旋转可视化
- **Phase C — general QA 验收(7/9-7/10,2 天)**:每件 model 改完 general **实际 dev server 跑**(画布连线 + Run + 看 model 行为)。不通过派对应人修(coder / UIer),闭环后 Phase D
- **Phase D — 整体回归 + cleanup(7/10-7/12,3 天)**:verifier 跑全量回归覆盖(决策 12 lint cleanup 同步);coder 清 `@ts-ignore` → `@ts-expect-error`
- **Phase E — 主理人部署(7/12-7/14)**:Vercel + Railway + prisma migrate deploy + Lighthouse 性能 > 70;内测脚本跑 5 学生(7/13-7/14,`docs/internal-test-script.md`)
- **Phase F — sprint 末(7/15-7/16)**:主理人最终验收;manager 整理 `docs/phase2-backlog.md`(10+ 条)+ docs/prd-sync-2026-07-02.md 收尾
- **Phase G — backlog 备查(不阻塞)**:simavr WASM 替换 JS 软仿真;课程编辑器(教师侧);真实邮件 / Sentry 真接(部署前主理人给 DSN)
- **派活分工**(谁都不漏,已 sync manager):
  - **coder**:Phase A 5 件 + Phase B 4 件 + Phase D lint cleanup(9 个独立 commit)
  - **UIer**:**无新 view 任务,不接活**(决策 21 v2 layout 已收口,idle 等触发)
  - **verifier**:Phase D 触发再跑覆盖回归,现在 idle
  - **general**:Phase C 实跑 + Phase E 部署后 Lighthouse + 5 学生内测
  - **manager**:Phase F sprint 末 phase2-backlog 整理 + Phase D/E 协调
  - **产品经理**(我):决策 13 文档化(本节)+ 监督各 Phase 进度 + 决策 12 lint cleanup 调度
- **PRD §14 验收影响**:修完后 PRD §14 "12 元件 + Run 实跑可观察" box 全打勾;§9 北极星(完课率 > 60%)依赖 Phase A 按钮 + 电位器 model(学生课内必用)
- **风险**:I2C 模拟(Wire 库 + SSD1306)Phase B OLED 是大头,如 1 天估时不准可能延;蜂鸣器 HTMLAudioElement 在 iOS Safari 兼容性需测,失败降级 console.log;超声波模拟距离精度不追求 1cm,P95 偏差 < 10% 即可

**待回写位置**:
- PRD §10 风险表加一条仿真精度风险
- PRD §14 验收清单打勾 + 加反馈/模板/fork/Sentry 四条
- `docs/decisions.md` 补 ADR-003/004/005

---

## 6. 代码活清单(已派给 reins,等排期)

| # | 任务 | 估时 | 优先级 |
|---|---|---|---|
| C1 | Sentry 接入(前端 init + 后端 init) | 0.5 天 | P0 |
| C2 | 模板库 3 个(`apps/web/src/templates/led-blink.ts` `button-read.ts` `pot-dimmer.ts`,编辑器顶部加下拉) | 1 天 | P0 |
| C3 | 分享页 fork 按钮 + 后端 `POST /api/projects/fork/:shareId` API | 1 天 | P0 |
| C4 | 反馈 mailto 入口(页脚按钮,内容预填) | 0.5 天 | P0 |
| C5 | (可选)P1:课程 step 加 hint1-3 / check 字段 | 0.5 天 | P1 |
| C6 | (可选)P1:分享链接 rotate shareId 按钮 | 0.5 天 | P1 |

总计 P0 ≈ 3 天,在 13 天 sprint 余量内吃得下。

---

## 7. 变更日志

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.0 | 2026-07-02 | 初稿。Review PRD v2.0 + 代码现状,输出脱节点 + 缺口 + 候选功能。 |
| v1.1 | 2026-07-02 | 产品决策落地(6 项)。追加 §6 代码活清单(P0 4 项 + P1 2 项)。原 §6 顺延为 §7。 |
| v1.2 | 2026-07-02 | **决策 7:PRD §14 AI 响应阈值从 "< 5s" 调整为 "P95 < 6s"。general 三轮 30 次调用数据为依据,B 路径失败,C 路径产品经理签字。** |
| v1.3 | 2026-07-02 | **决策 8:CoursePlayer check 字段从 Phase 2 提前到 MVP 落地。完课率是 PRD §9 唯一北极星,check 字段是任务卡驱动关键。Step interface 加可选 check 字段,不破坏 4 字段 schema。UIer 接 D11-D15 实施。** |
| v1.4 | 2026-07-02 14:18+ | **决策 9:元件库扩展 8 → 12(OLED / MPU6050 / RGB LED / 七段)。主理人 review 元件库太少;补齐 PRD §4.1 原计划的 12 件;coder 并行做;估时 2-4 天。决策 10:UNO 板 1:1 还原(view 层重画,不动 pin 定义)。主理人 review 当前 UNO 视觉粗糙;参考 https://wokwi.com/arduino;UIer 接;估时 0.5-1 天。Sprint 余 13 天够吃。** |
| v1.5 | 2026-07-02 15:03+ | **决策 11:UNO 板严格 1:1 还原(决策 10 升级)。主理人验收 369877f 失败,"跟真实的不一样"。PM verify 失误(只看代码 pin 数对,没真看渲染)。决策 11 严格化:除 pin 还要 layout / 比例 / USB / POWER jack / POWER 底边横排 / ATmega / 晶振 / LED / 复位 / ICSP 全按 Wokwi reference。UIer 重做,1-2 天。verify 改进:实际开屏看渲染。** |
| v1.6 | 2026-07-02 21:22+ | **决策 12:Lint cleanup(`@ts-ignore` → `@ts-expect-error`)。verifier 建议,sprint 末 Phase D 调度,coder 0.5 天。决策 13:PRD §14 元件 model 补全(Phase A 5 件 + Phase B 4 件)。**主理人 19:56 拍板 B 路径"补 model 再部署",manager 协调 13 天 plan。Phase A 修 5 PRD §14 原件 model(canvas 拖 / Run 看 model 行为),Phase B 升级 4 决策 9 元件 model(stub → 完整),Phase C-D QA + 回归,Phase E 主理人部署(Vercel + Railway + Lighthouse + 5 学生),Phase F sprint 末验收 + phase2-backlog 整理。派活分工:coder 9 件 9 commit + UIer idle + verifier idle + general 实跑 + manager 协调 + PM 监督。** |
| v1.7 | 2026-07-03 07:23 | **决策 22 UI 重构 v0/v1/v2**:主理人 9:18 dev server review 3 件 P0(1/3 编码布局 / wire z-index / 11 件真图化)。78bd923 (v0 编码区 1/3 + wire z-index) → 204baf2 (v1 UIer 列表→dropdown,违反"下拉式改没了"主理人原话,PM 误判,UIer revert) → 5983661 (v2 FAB 元件库 0 常驻,保留 dropdown 选项逻辑)。**决策 13 9 件 model 全交付**:Phase A 5 件(按钮/电位器/舵机/蜂鸣器/超声波)+ Phase B 4 件(MPU6050/OLED/RGB LED/七段),coder 4 commit 链 af98027/591f177/724de56/1eed317 verifier 全 PASS。**流程观察(主理人 22:18 P0 反馈触发)**:决策 24 P0 AI 助教 UX 重设计 — 现在的 AI 助教没输入地方,学生问不到;学生应该有对话框主动问 AI,AI 读当前实验项目代码/报错/wiring/parts,可帮学生连线(Phase 2);vs 外部 AI 豆包/ChatGPT 的最大区别是有项目上下文。详见下方"决策 24 详情"。**general 主动 push 机制认可**(实战有效 3 次:b5b25af 性能 / af54869 label text / 56fg90 click interception);**PM QA 协调漏洞**:sprint 末 general 实跑没强制覆盖 invalid connection 案例(连不存在的引脚/跨类型/地线接 5V 等),主理人 10:22 dev server 上手发现 → P0 修复后补 invalid 测试。**PM 派活前 sync manager 闭环**:manager 协调全局可见性需要派活意图 + commit 落地都同步 |
| v1.8 | 2026-07-03 16:48 | **决策 22 v3 11 件 view 真图化 + 决策 23 drag hit area + wiring P0 闭环**:5641e16 (决策 22 v3 batch 1 LED/电阻真图化:圆灯珠+色环+轴向引脚)+ 44f2e6f (wiring 校验 P0:12 件 pinType 6 类标注 + 兼容矩阵 i2c↔非i2c 拒绝 + 11 valid + 6 invalid case 测试)+ af54869 (general 自 push 真问题:use g.querySelector("text") 替代 lastElementChild,appendAll 后取错)+ 856ce90 (general 自 push:装饰 SVG 元素 pointer-events 修 click 穿透,但过度修到 data-pin 致 drag 失效)+ f40a44e (决策 23 drag hit area 修复,856ce90 v2 data-pin 重建 hit area)。PR merge 流程:UIer dd732ee + coder d0148a7 cherry-pick → main a12032a 同步 → coder emoji 兼容 d920652 |
| v1.9 | 2026-07-03 23:35 | **决策 24 v1 实施收口**(主理人 20:18 P0 反馈 + 拍板):6 commit 链 dd732ee (UIer 抽屉初版:drawer + Cmd+I + 4 state tabs read-only + 多轮 chat 流式 + user/AI 气泡)→ 79fb6d8 (UIer 抽屉收尾 onAsk 联调 + suggestions 卡片展示)→ a12032a (coder /api/ai/chat-context 接 projectState { code/errors/wirings/parts } + DeepSeek V3 prompt 注入 + 返回 { answer, suggestions }; rate limit 20 次/天复用;taskType='chat')→ d920652 (coder parseSuggestions emoji-tolerant 兼容 → 实际 AI 输出 1-line [type] [target] [payload] 不是 multi-line,parser 期望多行)→ 126c9fe (coder basePrompt 强制 ## 💡 建议 + ## 提示 两个结构化块:suggestion 类型 [type: code/wiring/part] [target] [描述])→ 23c3c9f (coder emoji-tolerant parseSuggestions 加 fallback + correct tests)。**general 20/20 PASS**(决策 24 v1 收口)。**端点命名**:`/api/ai/chat-context`(不是 /api/ai/chat)更准 function intent(v1 读 context + 给建议)。**v1 范围**:只读 + 给建议,v2 写+action(Phase 2)。**对齐 PRD §6.7** 新增 AI 助教主动对话章节(字面执行"对话框"→ 右侧 drawer,与决策 22 FAB 不占空间哲学一致)|

**决策 24 详情**(AI 助教 UX 重设计 — 主理人 20:18 P0 反馈,产品经理拍板):

- **触发**:主理人 dev server 上手测 AI 助教,UX 上**没有输入框** — 学生无法主动问 AI。PRD §6.4 / §6.5 的 explain / error / hint 是被动触发,缺主动对话入口
- **主理人原话**:现在的 AI 助教设计有问题,根本没有输入的地方;我的想法应该就是一个对话框,学生输入问题,AI 直接回答;AI 可以直接读取当前实验项目的代码、报错、状态,可以帮助学生连线;明白了吗,跟学生问外部的豆包之类 AI 的区别
- **3 个核心差异点**(主理人强调):
  1. **有输入的地方** — 主动对话 input box
  2. **AI 能读当前项目状态** — code / errors / wiring / parts
  3. **帮学生连线** — 可选 v2 (v1 只读 + 给建议)
- **拍板 UX 形态**:右侧 Drawer(字面执行"对话框"+ 沿用决策 22 "不占画布空间")+ Cmd+I 快捷键 + 底部 textarea + Enter 发送 / Shift+Enter 换行 + 状态栏 + 流式响应
- **AI state 范围 v1 必含**:`code(CodeMirror buffer)` + `errors(compile/runtime)` + `wirings(JSON 数组)` + `parts(name + position + pin 状态)`
- **server API**:`POST /api/ai/chat-context` 接收 `{ projectId, message, state }` → DeepSeek V3 prompt 注入 state context → 返回 `{ answer, suggestions }`(rate limit 20 次/天复用)
- **6 commit 收口链路**:dd732ee(UIer 抽屉初版)→ 79fb6d8(UIer onAsk 联调)→ a12032a(coder chat-context)→ d920652(emoji parse 兼容)→ 126c9fe(basePrompt 强制结构化)→ 23c3c9f(emoji-tolerant parser + 测试)
- **general 20/20 PASS**(主理人 dev server 视觉验收待)
- **归档要点**:主理人主动设计 UX 反馈(决策权在他);沟通时不挑选项,字面执行主理人原话;"对话框"字面 = 右侧 drawer
- **风险/Phase 2 备查**:DeepSeek prompt context token 数;rate limit 20 次/天可能不够;流式 SSE 复杂度(v1 轮询 / v2 streaming)
| v1.10 | 2026-07-05 09:57+ | **决策 25:AI 助教改成始终可见的对话框(回退决策 24 v1 部分内容)。**主理人 9:57 P0 反馈:Cmd+I 抽屉(默认关闭)不对,字面执行不到位 — 主理人 20:18 原话"我的想法应该就是一个对话框" = 一个对话框 = **始终可见**(默认显示,不需快捷键打开)。详见下方"决策 25 详情" |

**决策 25 详情**(AI 助教改始终可见对话框 — 主理人 9:57 P0 反馈,字面执行回退决策 24 v1):

- **触发**:主理人 dev server 上手测决策 24 v1 实施结果(dd732ee/79fb6d8 抽屉),发现 "Cmd+I 快捷键打开抽屉(默认关闭)" 与原话"对话框"字面意图不符
- **主理人字面原话**(7/3 20:18):"我的想法应该就是一个对话框,学生输入问题,AI 直接回答"
- **PM 字面执行不到位**(v1.8 拍板 = 抽屉 + Cmd+I + 默认关闭):
  - **问题点 1:抽屉 ≠ 对话框**。抽屉是"按需弹出的容器",对话框是"始终可见的窗口"
  - **问题点 2:Cmd+I 快捷键 ≠ 对话框应有的操作**。快捷键是在"抽屉关着"前提下的打开手段,但主理人字面要的是"对话框始终在"学生不需要任何操作就能看到 AI 入口
  - **问题点 3:决策 22 FAB 0 常驻哲学被错误延伸**。FAB 0 常驻是主理人拍板的(元件库),但 AI 助教不是元件库,不能用 0 常驻
- **拍板(回退决策 24 v1 部分)**:
  - ✅ 保留:右侧 Drawer 的 state 范围(code/errors/wirings/parts) + server `/api/ai/chat-context` + 多轮 chat 流式 + suggestions + user/AI 气泡 + 底部 textarea + 状态栏 + 流式响应 + Cmd+I / Ctrl+I(保留,非必)
  - ❌ 移除:Cmd+I 快捷键作为**唯一打开方式**(现在是"附加打开方式",但默认始终显示)
  - 🆕 新增:**始终可见对话框**(默认显示,不关闭,学生随时能问)
- **新 UX 形态**:
  - **始终可见**:右侧抽屉默认展开,✕ 按钮可选关闭(但仍可看到 AI 入口图标或气泡提示)
  - **快捷键保留**:Cmd+I / Ctrl+I 仍可 toggle(收缩/展开),但不是必选入口
  - **画布空间权衡**:右侧抽屉始终可见会占 ~30% 画布宽度。这是 v1 必要的 trade-off,主理人字面意图优先。画布缩到 70% 即可。
- **派活**:
  - **UIer**:回退 dd732ee 抽屉默认行为 + 加始终可见 + toggle 快捷键保留 — 估时 0.5 天
  - **verifier**:全量回归 + visual 验证(抽屉始终可见)
  - **general**:dev server 实跑"AI 助教显示了吗"
  - **PM**(我):v1.10 changelog 落档(已写) + 字面执行 lesson 归档
- **PRD 影响**:
  - §6.7 决策 24 v1 形态描述(右侧 drawer + Cmd+I)需补:Cmd+I 是 toggle,非必
  - 🆕 §6.8 新增:AI 助教**始终可见对话框**(主理人字面意图)
- **字面执行 lesson learned(二次案例)**:
  - v1.8 决策 24 我拍"抽屉 + Cmd+I + 默认关闭"是字面"对话框"翻译错误。"对话框" = 一个对话框 = 始终可见 = 不是 drawer
  - 同 memory 教训回顾:dropdown 误判"改没了"也是字面不到位。**主理人用"对话框"≠ 我理解的"抽屉"**,中间的过程性推断需要更接近字面意思
  - **改进**:字面执行 → 中间不推断,优先读主理人字面原话的字面意思(对象、动作、状态);主理人用"对话框"= 字面"对话框"
  - **流程改进**:PM 拍板 UX 形态时,在 prd-sync changelog 显式列"主理人原话 → PM 字面理解 → 派活描述",三段对应,避免中间推断
- **对齐 PRD §6.8**:AI 助教始终可见对话框 + /api/ai/chat-context + state 注入 + v1 读+建议 / v2 写+action
